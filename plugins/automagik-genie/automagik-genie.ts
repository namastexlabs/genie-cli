// OpenClaw standalone extension entrypoint.
// Install via: openclaw plugins install <this-file>
// Manifest is resolved from the same directory: openclaw.plugin.json

import register from "./index";

export const id = "automagik-genie";

export default register;
